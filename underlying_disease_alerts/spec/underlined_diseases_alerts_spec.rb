require 'spec_helper'

describe UnderlinedDiseasesAlerts do
  it 'has a version number' do
    expect(UnderlinedDiseasesAlerts::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
